Here's a step-by-step guide to download and test everything:

## **Step 1: Create Project Folder**

Create a new folder for your project:
```bash
mkdir flask-mysql-form
cd flask-mysql-form
```

## **Step 2: Save All Files**

Create these files in your project folder:

### **1. Save `index.html`**
Copy the HTML code from the artifact above and save it as `index.html`

### **2. Save `app.py`**
```python
from flask import Flask, request, jsonify
from flask_cors import CORS
import mysql.connector
from mysql.connector import Error

app = Flask(__name__)
CORS(app)

# Database configuration
DB_CONFIG = {
    'host': 'localhost',
    'database': 'your_database_name',
    'user': 'your_username',
    'password': 'your_password'
}

def get_db_connection():
    try:
        connection = mysql.connector.connect(**DB_CONFIG)
        return connection
    except Error as e:
        print(f"Error connecting to MySQL: {e}")
        return None

@app.route('/submit', methods=['POST'])
def submit_data():
    try:
        data = request.get_json()
        
        if not data.get('name') or not data.get('email'):
            return jsonify({
                'success': False,
                'message': 'Name and email are required'
            }), 400
        
        connection = get_db_connection()
        if connection is None:
            return jsonify({
                'success': False,
                'message': 'Database connection failed'
            }), 500
        
        cursor = connection.cursor()
        query = """
            INSERT INTO users (name, email, phone, message)
            VALUES (%s, %s, %s, %s)
        """
        values = (
            data.get('name'),
            data.get('email'),
            data.get('phone', ''),
            data.get('message', '')
        )
        
        cursor.execute(query, values)
        connection.commit()
        cursor.close()
        connection.close()
        
        return jsonify({
            'success': True,
            'message': 'Data saved successfully'
        }), 201
        
    except Error as e:
        return jsonify({
            'success': False,
            'message': f'Database error: {str(e)}'
        }), 500
    except Exception as e:
        return jsonify({
            'success': False,
            'message': f'Server error: {str(e)}'
        }), 500

@app.route('/health', methods=['GET'])
def health_check():
    return jsonify({'status': 'healthy'}), 200

if __name__ == '__main__':
    app.run(debug=True, port=5000)
```

### **3. Save `requirements.txt`**
```txt
Flask==3.0.0
flask-cors==4.0.0
mysql-connector-python==8.2.0
```

### **4. Save `setup.sql`**
```sql
CREATE DATABASE IF NOT EXISTS my_form_db;

USE my_form_db;

CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    phone VARCHAR(20),
    message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## **Step 3: Install MySQL**

If you don't have MySQL installed:

**Option A: MySQL Server**
- Download from: https://dev.mysql.com/downloads/mysql/
- Install and note your root password

**Option B: XAMPP/WAMP (Easier for beginners)**
- XAMPP: https://www.apachefriends.org/
- Includes MySQL, phpMyAdmin (GUI)

## **Step 4: Set Up Database**

**Using MySQL Command Line:**
```bash
mysql -u root -p
# Enter your password, then:
source setup.sql
exit
```

**Using phpMyAdmin (if using XAMPP):**
1. Open http://localhost/phpmyadmin
2. Click "SQL" tab
3. Paste contents of `setup.sql`
4. Click "Go"

## **Step 5: Update Database Credentials**

Edit `app.py` and update the `DB_CONFIG`:
```python
DB_CONFIG = {
    'host': 'localhost',
    'database': 'my_form_db',        # ← Change this
    'user': 'root',                   # ← Your MySQL username
    'password': 'your_password_here'  # ← Your MySQL password
}
```

## **Step 6: Install Python Dependencies**

Open terminal/command prompt in your project folder:

```bash
# Create virtual environment (recommended)
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On Mac/Linux:
source venv/bin/activate

# Install packages
pip install -r requirements.txt
```

## **Step 7: Run the Flask Server**

```bash
python app.py
```

You should see:
```
* Running on http://127.0.0.1:5000
* Debug mode: on
```

## **Step 8: Open the HTML Form**

Simply **double-click** `index.html` or drag it into your browser.

The form should open and be ready to use!

## **Step 9: Test It**

1. Fill out the form
2. Click "Submit Data"
3. You should see "Data saved successfully!"
4. Check your database to verify the data was inserted

**Check database via command line:**
```bash
mysql -u root -p
USE my_form_db;
SELECT * FROM users;
```

## **Troubleshooting:**

### **"Connection error" in browser:**
- Make sure Flask server is running (`python app.py`)
- Check the URL in HTML matches Flask (http://localhost:5000)

### **"Database connection failed":**
- Verify MySQL is running
- Check credentials in `app.py`
- Test connection: `mysql -u root -p`

### **CORS errors:**
- Make sure `flask-cors` is installed
- Check browser console (F12) for specific errors

### **Import errors:**
- Make sure virtual environment is activated
- Reinstall: `pip install -r requirements.txt`

## **Quick Test Script**

Create `test.py` to test the backend:
```python
import requests

data = {
    "name": "Test User",
    "email": "test@example.com",
    "phone": "123-456-7890",
    "message": "This is a test"
}

response = requests.post('http://localhost:5000/submit', json=data)
print(response.json())
```

Run: `python test.py`

Need help with any specific step?